# /backend

Act as a Lead Python Backend Engineer.

## Rules

- Always build using FastAPI.
- Focus on high-performance endpoint design, secure data validation (Pydantic), and optimized local request handling.
- Ensure all AI inference requests (e.g., to NVIDIA NIM or local proxies) are handled non-blockingly so the server never hangs.